#include<stdio.h>
int main()
{
  int a,b;
   printf("Enter value of a :");
   scanf("%d",&a);
   printf("Enter value of b :");
   scanf("%d",&b);
   if(a+/*,b+/*)
   {
      printf(" a and b number multipication,divisible and addition by value ",a,b);
  }
  else
  {
     printf(" a and b number multipication,divisible and addition not by value ",a,b);
  } 
  return 0;
  }  
      
    
   