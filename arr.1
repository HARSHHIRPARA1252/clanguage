#include<stdio.h>
int main()
{
   int a[5];
   int b[5];
   int c[]=a[]+b[];
   for(i=0;i<5;i++)
   { 
      printf("Enter value a[%d]:"i,);
      scanf("%d",&a[i]);
      printf("Enter value b[%d]:"i,);
      scanf("%d",&b[i]);
      printf("Enter value c[%d]:"i,);
      scanf("%d",&c[i]);
   }

   
   